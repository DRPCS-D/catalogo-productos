# Secretos y credenciales — NUNCA subir al repo
.env
.env.*
*.env
service-account.json
.mcp.json
*.key
*.pem

# Apps Script (vive en script.google.com, no en el repo)
Code.gs
# admin.html SE incluye en el repo (versión PWA con PIN auth + Supabase).
# Si quisieras ocultarlo del deploy, agrega "admin.html" acá.

# Sync server (vive en el servidor Docker, no en el repo)
sync-server/

# Sistema operativo
.DS_Store
Thumbs.db
desktop.ini

# Editor
.vscode/
.idea/
*.swp
*~

# Build / cache locales
node_modules/
*.log

# Planes de Claude (privados del workspace)
.claude/

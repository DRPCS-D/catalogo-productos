# Secretos y credenciales — NUNCA subir al repo
.env
.env.*
*.env
service-account.json
.mcp.json
*.key
*.pem

# admin.html SE incluye en el repo (versión PWA con PIN auth + Supabase).
# Si quisieras ocultarlo del deploy, agrega "admin.html" acá.

# Sync server: el código fuente vive en el repo; los secretos NO.
# Las reglas de arriba (service-account.json, .env, .env.*) ya cubren
# los archivos sensibles dentro de sync-server/.

# Sistema operativo
.DS_Store
Thumbs.db
desktop.ini

# Editor
.vscode/
.idea/
*.swp
*~

# Build / cache locales
node_modules/
*.log

# Planes de Claude (privados del workspace)
.claude/
